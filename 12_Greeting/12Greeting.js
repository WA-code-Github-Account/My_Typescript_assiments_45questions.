"use strict";
let names = ['Afifa', 'Warda', 'Bilques', 'sadiqa',];
const message = ("Welcome to typescripts class: ");
for (let index = 0; index < names.length; index++) {
    console.log(message + names[index]);
}
